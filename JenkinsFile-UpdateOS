pipeline {
    agent {
        label 'ansible'
    }
    environment {
        GIT_HOME = tool name: 'Default', type: 'Tool'
    }
    stages {
        stage('Checkout Git Repository') {
            steps {
                sshagent(['git']) {
                    // Use the sshagent step to provide SSH credentials
                    sh "${GIT_HOME}/bin/git clone git@github.com:Goghtime/Lab.git"
                }
            }
        }

        stage('Run Ansible Playbook') {
            steps {
                // Your Ansible playbook execution step goes here
                sh "ansible-playbook -i /hosts /dnsmasq/update-dnsnasq-conf.yml"
            }
        }
    }
}
